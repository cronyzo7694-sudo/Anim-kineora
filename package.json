{
  "name": "anim-kineora-bhashasetu",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check worker.js && node --check static/app.js && python3 -m py_compile server.py",
    "dev": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
